{
  "name": "molvis",
  "version": "1.0.0",
  "description": "interactive molecule visulization package",
  "author": "Roy Kid",
  "license": "SEE LICENSE IN LICENSE",
  "workspaces": [
    "core",
    "standalone",
    "widget"
  ],
  "scripts": {
    "dev:core": "npm run -w core dev",
    "dev:widget": "vite widget",
    "dev:standalone": "npm run -w standalone dev",
    "build:core": "npm run -w core build",
    "build:widget": "npm run build -w widget",
    "build:standalone": "npm run build -w standalone"
  },
  "devDependencies": {
    "@anywidget/vite": "^0.2.0",
    "vite": "^5.4.11"
  },
  "dependencies": {
    "tslog": "^4.9.3"
  }
}
