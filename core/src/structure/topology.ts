/**
 * Minimal placeholder Topology without wasm dependency.
 * Extend as needed to mirror Scene.bonds if a separate abstraction is desired.
 */
export class Topology {
  // Intentionally minimal for now
}
