---
slug: data-source-synthesis-03-synthesis-core
criteria:
  - id: ac-001
    summary: synthesize extend concatenates atoms, offsets bonds, writes source_id ordinals
    type: code
    pass_when: |
      Test in core/tests/scene_synthesis.test.ts synthesizes two extend
      sources (3 + 2 atoms): result atoms block nrows() == 5, source_id
      column == [0,0,0,1,1], and second source's bond (0,1) appears as
      (3,4) in the merged bonds block.
    status: verified
    last_checked: 2026-05-31
  - id: ac-002
    summary: synthesize augment unions blocks with last-wins on name conflict
    type: code
    pass_when: |
      Test synthesizes two augment sources sharing a block name; the
      result block equals the later source's block (last-wins), atom
      count is unchanged, and no source_id column is present.
    status: verified
    last_checked: 2026-05-31
  - id: ac-003
    summary: length-1 source broadcasts to every timeline index
    type: code
    pass_when: |
      With a length-1 source and a length-3 source, synthesize at
      frameIndex 0,1,2 each yields a frame whose broadcast-source atoms
      equal that source's frame(0) coordinates at all three indices.
    status: verified
    last_checked: 2026-05-31
  - id: ac-004
    summary: equal-length sources zip frame k from each
    type: code
    pass_when: |
      Two length-3 sources synthesized at frameIndex=k draw frame(k)
      from each source (verified by per-frame distinct coordinates).
    status: verified
    last_checked: 2026-05-31
  - id: ac-005
    summary: unequal lengths both >1 throws a naming error
    type: code
    pass_when: |
      Synthesizing a length-2 and a length-3 source throws; the error
      message contains both lengths (2 and 3) and the mismatched source id.
    status: verified
    last_checked: 2026-05-31
  - id: ac-006
    summary: single enabled source passes through unchanged with no source_id
    type: code
    pass_when: |
      With exactly one enabled source, synthesize returns a frame whose
      atoms columns equal source.frame(frameIndex) and the atoms block
      has no source_id column.
    status: verified
    last_checked: 2026-05-31
  - id: ac-007
    summary: alignment ON restores a rotated source onto the reference (rmsd≈0)
    type: scientific
    pass_when: |
      A second source generated by applying a known rotation to the
      reference source's coords is synthesized with alignment.enabled;
      its post-alignment coords match the reference within spec-01
      tolerance (each atom |Δ| < 1e-6 Å) and its reported rmsd < 1e-6.
    status: pending
  - id: ac-008
    summary: mass-weighted alignment forwards weights to superpose (rmsd≈0)
    type: scientific
    pass_when: |
      With a mass column and massWeight=true, the rotated source aligns
      to the reference and reported rmsd < 1e-6, confirming weights are
      passed through to superpose.
    status: pending
  - id: ac-009
    summary: alignment with atom-count mismatch and no subset throws clearly
    type: code
    pass_when: |
      alignment.enabled with subset=null and two sources of differing
      atom counts throws; the message names both atom counts and the
      offending source id.
    status: verified
    last_checked: 2026-05-31
  - id: ac-010
    summary: exported types are serializable and synthesize is pure
    type: code
    pass_when: |
      SceneSynthesisConfig / SynthesisSource / SynthesisAlignment are
      exported from core/src/system/scene_synthesis.ts; a test snapshots
      input frame columns before synthesize and asserts they are
      unchanged after (no input mutation).
    status: verified
    last_checked: 2026-05-31
  - id: ac-011
    summary: full check and test suite pass
    type: runtime
    pass_when: |
      `npm run typecheck`, `npm run lint`, and `npm test` all exit 0 with
      the new module and test file present.
    status: verified
    last_checked: 2026-05-31
---

# Acceptance criteria

- **ac-001 … ac-006, ac-009, ac-010** (`code`): structural behaviors of `synthesize` verifiable by reading the rstest assertions in `core/tests/scene_synthesis.test.ts` — no runtime host needed (pure module, mock frames).
- **ac-007, ac-008** (`scientific`): domain validation of the Kabsch alignment path; bind to spec-01 tolerance (atom displacement and RMSD thresholds) so a third party confirms the `superpose`→`applyTransform` integration restores rotated geometry.
- **ac-011** (`runtime`): gate that the workspace typechecks, lints, and the full test suite passes with the new files in place.
