/**
 * `@molcrafts/molvis/plugin` — public plugin authoring SDK.
 *
 * Re-exports `@molcrafts/molvis-plugin`. Plugin authors must import from
 * this path (or the package name), never from monorepo `page/` internals.
 */
export * from "@molcrafts/molvis-plugin";
