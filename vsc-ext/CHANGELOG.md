# Changelog

## [0.0.2]

- Add Quick View, custom editor, and full editor workspace flows
- Add Zarr directory support for trajectory data
- Add reload/save commands and webview state handling improvements
- Expose `molvis.config` and `molvis.settings` configuration overrides
