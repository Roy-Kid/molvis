{
  "name": "@molvis/python",
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "build": "rslib build",
    "dev": "rslib build --watch",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && python3 -m pytest tests"
  },
  "devDependencies": {
    "@anywidget/types": "^0.2.0",
    "@rsbuild/core": "^1.7.0",
    "@rslib/core": "^0.18.0",
    "@types/node": "^24.10.1",
    "anywidget": "^0.9.21",
    "typescript": "^5.9.3"
  }
}
